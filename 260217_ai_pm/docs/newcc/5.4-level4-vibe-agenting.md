---
type: guide
status: new
track: ecosystem
---

# 5.4 Level 4 - Vibe Agenting

**빠른 요약**
- 5.4 Level 4 - Vibe Agenting 주제를 빠르게 이해하고 바로 써볼 수 있게 정리한 문서입니다.

**이번 문서 목표**
- 읽고 나면 5.4 Level 4 - Vibe Agenting 내용을 내 업무 흐름에 맞게 적용할 수 있습니다.

**처음 보는 분 가이드**
- 먼저 `## 1) 목적`과 `## 5) 실습/적용 체크리스트`만 읽고 시작하세요.
- 용어가 낯설면 [사전](./사전.md)을 먼저 확인하세요.

## 1) 목적
- `/agent` 기반 Vibe Agenting으로 PRD→TDD→Eval→Prototype→배포 핸드오프를 체계화한다.
- 단일 작업자 생산성을 넘어, 팀 단위 병렬 실행과 품질 게이트를 운영한다.
- 실험적 프로토타이핑을 운영 가능한 제품 후보로 전환한다.

## 2) 원문 핵심 요약 (ccforpms)
- 원문 1.4는 병렬 agents로 대량 작업을 분산하고 synthesis로 통합하는 패턴을 제시한다.
- 원문 1.5는 custom sub-agent를 통해 역할 기반 리뷰/생성을 반복 가능한 시스템으로 만든다.
- 원문 2.1은 다중 관점 리뷰(기술/비즈니스/사용자)로 문서 품질을 높이는 방법을 보여준다.
- 원문 4.2~4.5는 계획→구현→반복→GitHub→배포까지 end-to-end 실행 루프를 실습한다.
- 즉, Level4는 “아이디어 생성”이 아니라 “역할 분업 + 검증 + 출시 연결”이 핵심이다.

## 3) 이든 철학 반영 포인트 (v2.4)
### Dennis baseline vs 이든 확장
| 항목 | Dennis baseline | 이든 Level4 운영 |
|---|---|---|
| 에이전트 사용 | 생산성 보조 | 역할 기반 오케스트레이션 체계 |
| 산출물 | 문서/코드 일부 | README/instructions/eval/prototype 패키지 |
| 품질 보증 | 수동 점검 | 단계별 Gate(요구사항/테스트/평가/배포준비) |
| 배포 연결 | 별도 작업 | n8n 핸드오프까지 포함한 운영 파이프라인 |

- **FR-019 / AC-013** 직접 반영: `/agent` 결과물 구조를 표준화.
- **P3/P4**: PM은 역할과 품질 게이트를 설계하고, 에이전트는 구현/분석/초안을 담당.
- **P10**: 다중 가설을 병렬로 검증해 Validation Gap을 줄이고, 의사결정 전 학습 속도를 높인다.
- **P9**: 문서 명세가 실제 워크플로우로 변환되는 구조 유지.

## 4) 실무 적용 시나리오
### 실행 순서 (3~4주)
1. **Stage A: PRD 정제**
   - 입력: 문제 정의, KPI, 제약
   - 산출물: 확정 PRD + 비목표 + 리스크
2. **Stage B: TDD/Eval 설계**
   - 입력: PRD
   - 산출물: 테스트 시나리오, Eval 기준, 합격 조건
3. **Stage C: Prototype 생성**
   - 실행: `/agent`로 프로토타입 구현
   - 산출물: 실행 가능한 MVP + 데모 가이드
4. **Stage D: 평가 및 수정**
   - 실행: Eval 기준으로 품질 측정
   - 산출물: 개선 이력, 실패 케이스
5. **Stage E: 운영 핸드오프**
   - 실행: n8n 또는 운영 시스템으로 배포/알림 연결
   - 산출물: 배포 체크리스트 + 모니터링 링크

### 실패 패턴
- 실패1: 에이전트 역할 경계가 모호해 중복 작업 발생
  - 대응: 역할별 입력/출력/책임 범위를 문서화
- 실패2: 프로토타입은 있으나 평가 기준 부재
  - 대응: 구현 전 Eval 항목 선고정(TDD/Eval 선행)
- 실패3: 배포 전 운영 고려 누락
  - 대응: 핸드오프 체크리스트(로그/알림/롤백) 강제
- 실패4: 데모 중심으로 끝나고 재현성 없음
  - 대응: 실행 스크립트와 환경 요구사항을 README에 포함

### 이번 주 완료형 실행 플랜
- **월~화:** PRD·비목표·성공지표를 확정하고 agent 역할 정의서(입력/출력/책임)를 작성한다.
- **수~목:** `/agent`로 프로토타입 1건을 생성하고 Eval 기준으로 1차 품질 측정을 수행한다.
- **금 검증:** Gate 통과 여부를 판단해 배포 핸드오프 가능 상태(또는 보완 항목)를 결정한다.
- **Done 기준:** agenting 패키지(README/instructions/eval/prototype), Gate 기록, 핸드오프 체크리스트가 모두 완성된다.
- **리스크·완화:** 역할 경계 중복으로 비효율이 생길 수 있으므로 책임 매트릭스(RACI 유사)를 먼저 고정하고 중복 산출물을 금지한다.

## 5) 실습/적용 체크리스트
- [ ] `/agent` 실행 전 PRD/비목표/성공지표를 확정했다.
- [ ] 역할별 agent 책임 범위를 명시했다.
- [ ] Eval 기준과 합격선(pass/fail)을 수치로 정의했다.
- [ ] 프로토타입 결과를 실행 가능한 형태로 저장했다.
- [ ] 운영 핸드오프(배포/모니터링/롤백) 문서를 완료했다.

## 6) 산출물
- Agenting 패키지(README, instructions, eval, prototype)
- 단계별 Gate 통과 기록(증빙 링크 포함)
- 배포 핸드오프 문서 및 운영 모니터링 링크
- Agent 역할 책임 매트릭스(역할/입력/출력/승인자)
